# Next Development Plan

## 現在の状態

**バージョン**: 0.1.0-beta.3
**最終更新**: 2026-02-03

### 最近完了した機能

- ✅ マルチボード機能の完全実装
- ✅ マルチボードバグ修正（ボード切り替え、状態永続化）
- ✅ カスタム列対応（新規タスクが最初の列に追加される）
- ✅ 通知機能（期限通知、繰り返しタスク生成通知）
- ✅ 繰り返しタスク機能
- ✅ ソフト削除とアーカイブ機能
- ✅ カレンダービュー
- ✅ リストビュー
- ✅ フィルター機能（タグ、担当者、優先度）
- ✅ レスポンシブデザイン

---

## 優先度別ロードマップ

### Phase 1: 高優先度（次のリリース候補）

#### 1.1 列の並び替え機能 (Column Reordering)
**優先度**: 高
**見積もり**: 2-3日
**説明**: 現在GripVerticalアイコンは表示されているが、ドラッグ&ドロップ機能は未実装

**実装タスク**:
1. `@dnd-kit/sortable`を使用して列の並び替え機能を実装
2. `ColumnManager`コンポーネントに並び替えUIを追加
3. `reorderColumns`アクションを実装（既存のアクションを活用）
4. 列の順序変更時に`saveToStorage()`を呼び出し
5. テスト追加（ユニット + E2E）

**技術的詳細**:
- `useSortable`フックを使用
- `columnOrder`配列を更新
- タスクの位置には影響しない（列内の相対位置は維持）

**成功基準**:
- [ ] 列をドラッグ&ドロップで並び替えられる
- [ ] 並び替えが永続化される
- [ ] モバイルでも操作可能
- [ ] テストカバレッジ: 90%以上

---

#### 1.2 日付範囲フィルター (Date Range Filter)
**優先度**: 高
**見積もり**: 1-2日
**説明**: react-day-pickerは既にインストール済み。FilterPanelに統合する

**実装タスク**:
1. `FilterPanel`に日付範囲選択UIを追加
2. `FilterState`に`dateRange`フィールドを追加
3. `useFilteredTasks`フックで日付範囲フィルターを実装
4. カレンダービューとの連携
5. テスト追加

**技術的詳細**:
```typescript
interface FilterState {
  tagIds: string[];
  assigneeIds: string[];
  priorities: Priority[];
  dateRange?: { start: Date; end: Date }; // 追加
}
```

**成功基準**:
- [ ] 日付範囲でタスクをフィルタリングできる
- [ ] カレンダービューから日付範囲を選択できる
- [ ] フィルターがクリアできる
- [ ] テストカバレッジ: 90%以上

---

### Phase 2: 中優先度（機能強化）

#### 2.1 ダークモード (Dark Mode)
**優先度**: 中
**見積もり**: 2-3日
**説明**: システム設定に従うダークモード対応

**実装タスク**:
1. Tailwind CSSのdark modeクラスを有効化
2. `app/globals.css`にダークモードの色定義を追加
3. テーマ切り替えコンポーネントを実装
4. `localStorage`にテーマ設定を保存
5. システム設定との連携
6. 全コンポーネントのダークモード対応
7. テスト追加

**技術的詳細**:
- `next-themes`または手動実装
- CSS変数を使用したテーマ管理
- `prefers-color-scheme`メディアクエリ対応

**成功基準**:
- [ ] ライト/ダーク/システム の3モード対応
- [ ] 全コンポーネントが読みやすい
- [ ] 設定が永続化される
- [ ] テストカバレッジ: 80%以上

---

#### 2.2 統計ダッシュボード (Statistics Dashboard)
**優先度**: 中
**見積もり**: 3-4日
**説明**: タスクの完了率、生産性メトリクスの可視化

**実装タスク**:
1. 新しいビュー「統計」を追加
2. 統計データを計算するヘルパー関数を実装
   - 完了率（日別、週別、月別）
   - 列ごとのタスク分布
   - 担当者ごとの作業量
   - 優先度別の傾向
3. チャートコンポーネントを実装（recharts or chart.js）
4. 期間選択機能
5. テスト追加

**技術的詳細**:
```typescript
interface Statistics {
  completionRate: number;
  tasksByColumn: Record<string, number>;
  tasksByAssignee: Record<string, number>;
  tasksByPriority: Record<Priority, number>;
  completedTasksTrend: { date: string; count: number }[];
}
```

**成功基準**:
- [ ] 視覚的に分かりやすいチャート
- [ ] リアルタイム更新
- [ ] 期間選択が可能
- [ ] エクスポート機能（CSV/PNG）
- [ ] テストカバレッジ: 80%以上

---

### Phase 3: 低優先度（将来的な機能）

#### 3.1 SharePoint同期 (SharePoint Sync)
**優先度**: 低
**見積もり**: 5-7日
**説明**: SharePointリストとの双方向同期

**実装タスク**:
1. SharePoint API統合
2. 認証フロー実装（OAuth 2.0）
3. 同期ロジック実装
   - プッシュ（ローカル → SharePoint）
   - プル（SharePoint → ローカル）
   - 競合解決
4. 同期設定UI
5. エラーハンドリング
6. テスト追加

**技術的詳細**:
- `@pnp/sp`ライブラリ使用
- `SyncMetadata`型を活用
- バックグラウンド同期

**成功基準**:
- [ ] SharePointリストと同期できる
- [ ] 競合が適切に解決される
- [ ] オフライン対応
- [ ] テストカバレッジ: 85%以上

---

#### 3.2 パフォーマンス最適化 (Performance Optimizations)
**優先度**: 低
**見積もり**: 3-5日
**説明**: 大量のタスクでもスムーズに動作するように最適化

**実装タスク**:
1. React.memoで不要な再レンダリングを防止
2. useCallbackでコールバック関数をメモ化
3. 仮想スクロール（react-window）を実装
4. Lazy loadingでコンポーネントを遅延読み込み
5. パフォーマンス計測とボトルネック特定
6. テスト追加

**技術的詳細**:
- React DevTools Profilerで計測
- 目標: 1000タスクでも60FPS維持

**成功基準**:
- [ ] 大量タスクでも快適に動作
- [ ] 初期ロード時間: 2秒以内
- [ ] ビュー切り替え: 100ms以内
- [ ] メモリ使用量: 100MB以内

---

#### 3.3 デスクトップ通知の拡張
**優先度**: 低
**見積もり**: 1-2日
**説明**: 期限超過タスクの通知を追加

**実装タスク**:
1. 期限超過チェックロジック
2. 通知送信機能
3. 通知設定UIの拡張
4. テスト追加

**成功基準**:
- [ ] 期限超過時に通知が表示される
- [ ] 通知頻度を設定できる
- [ ] テストカバレッジ: 90%以上

---

## 既知の制約と技術的負債

### 技術的負債

1. **Tauri v1の使用**
   - Tauri v2へのアップグレードを検討（破壊的変更あり）
   - 見積もり: 2-3日

2. **ESLint未設定**
   - ESLintの設定を追加（TypeScript strict modeのみ現在）
   - 見積もり: 0.5日

3. **タイトル重複**
   - App.tsxとKanbanBoard.tsxで"Kanban Board"が重複表示
   - 見積もり: 0.5日

4. **列並び替えUIの不完全実装**
   - GripVerticalアイコンはあるが機能なし
   - → Phase 1.1で対応

---

## テスト戦略

### ユニットテスト
- **カバレッジ目標**: 85%以上
- **ツール**: Vitest
- **対象**: ストア、ヘルパー関数、カスタムフック

### 統合テスト
- **カバレッジ目標**: 80%以上
- **ツール**: Vitest + React Testing Library
- **対象**: コンポーネント、ビュー

### E2Eテスト
- **カバレッジ目標**: 主要フローをカバー
- **ツール**: Playwright
- **対象**: 重要なユーザーフロー
  - タスクの作成・編集・削除
  - ボード切り替え
  - フィルター適用
  - ビュー切り替え

---

## リリース計画

### v0.1.0-beta.4（予定: 2週間後）
- 列の並び替え機能
- 日付範囲フィルター
- バグ修正

### v0.1.0（正式版、予定: 1ヶ月後）
- ダークモード
- 統計ダッシュボード
- パフォーマンス最適化
- 全機能の安定化

### v0.2.0（予定: 2-3ヶ月後）
- SharePoint同期
- デスクトップ通知の拡張
- 新機能の追加

---

## 開発プロセス

### TDD (Test-Driven Development)
- t-wada形式でテストを先に書く
- 各フェーズの終わりにE2Eテストで確認

### コードレビュー
- React/UIの変更はVercel React Best Practiceスキルでレビュー
- すべての変更はCLAUDE.mdに記録

### ドキュメント更新
- 機能完了時にMDファイルを更新
- 最新の状態を維持

---

## 次のステップ

1. **即座に実行**:
   - 列の並び替え機能の実装開始
   - 技術的負債の解消（ESLint設定、タイトル重複修正）

2. **2週間以内**:
   - 日付範囲フィルターの実装
   - v0.1.0-beta.4のリリース

3. **1ヶ月以内**:
   - ダークモードの実装
   - 統計ダッシュボードの実装
   - v0.1.0の正式リリース

---

## フィードバック

このロードマップは開発の優先順位と方向性を示すものです。ユーザーフィードバックや技術的な制約に応じて、適宜調整します。

**最終更新**: 2026-02-03
